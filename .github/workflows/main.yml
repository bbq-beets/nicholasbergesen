name: main test
on: workflow_dispatch
jobs:
  first_job:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Hello world!'
  second_job:
    runs-on: ubuntu-latest
    needs: [first_job]
    environment: production
    steps:
      - id: step1
        run: echo 'deploy me!'
      - id: step2
        run: |
          sleep 10
          echo 'I woke up!'
      - id: step3
        run: echo 'marko!'
