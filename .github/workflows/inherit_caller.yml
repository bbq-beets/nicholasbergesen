name: inherit_caller
on:
  workflow_dispatch:
jobs:
  call_in_repo:
    uses: ./.github/workflows/inherit_called.yml
    secrets: inherit
  call_public:
    uses: bbq-beets/nicholasbergesen/.github/workflows/inherit_called.yml@main
    secrets: inherit
