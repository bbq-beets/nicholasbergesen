name: Matrix-Called
on:
  workflow_call:
    inputs:
      version:
        description: 'Environment name'
        default: '14.x'
        required: false
        type: string

jobs:
  called_job:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ inputs.version }}
